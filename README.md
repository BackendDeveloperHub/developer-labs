# developer-labs
# 1. Project folder-ku po
cd your-project-folder

# 2. Dependencies install pannunga (oru thadava mattum)
npm install

# 3. Dev server start pannunga
npm run dev

backend 
#python3 -m venv venv
#source venv/bin/activate
#pip install -r requirements.txt
#uvicorn app.main:app --reload
